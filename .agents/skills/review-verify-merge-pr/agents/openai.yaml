interface:
  display_name: "Review, Verify, and Merge PR"
  short_description: "Independently review and gate OpenKeyboard PRs"
  default_prompt: "Use $review-verify-merge-pr to review the exact OpenKeyboard pull-request head and carry an autonomous implementation through guarded readiness and merge unless an explicit opt-out applies."
